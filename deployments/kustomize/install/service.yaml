kind: Service
apiVersion: v1
metadata:
  name: xds-inventory-webapi
spec:
  selector:
    pod: xds-inventory-webapi-label
  ports:
  - name: http
    protocol: TCP
    port: 80
    targetPort: webapi-port
